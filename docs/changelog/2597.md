- Removed broken support of using static Boost due to incompatibility with Boost.Log. CMake will raise an error when `Boost_USE_STATIC_LIBS` is enabled.
- Fixed CMake requiring Boost unit test framework even when tests are disabled.
- Fixed CMake linking libprecice instead of the test binary against the Boost unit test framework.
