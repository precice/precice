- Changed profiling output format to a text based format, which is more space efficient than the JSON format.
- Added CMake option `PRECICE_FEATURE_PROFILING_COMPRESSION` to optionally enable LZMA compression of the text profiling.
