- Added configurable lower and upper bounds for scalar and vector data.
- Added post-processing methods to quasi-Newton acceleration to handle steps that would violate defined data bounds. Options for `on-bound-violation` are `discard`, `clamp` or `scale` QN steps. Default is to `ignore` violations.
